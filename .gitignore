# Claude Code local, per-machine state
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Local secrets - never commit. Only .env.example is tracked.
.env
.env.*
!.env.example

# Local SQLite database
*.db
*.db-journal
prisma/dev.db*
